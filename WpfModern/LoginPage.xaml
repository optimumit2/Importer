<Page x:Class="WpfModern.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="LoginPage">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Użytkownik:"/>
            <TextBox Grid.Column="1" x:Name="txtUserName"/>
            <TextBlock Grid.Row="1"  Text="Hasło:"/>
            <TextBox Grid.Column="1" Grid.Row="1" x:Name="txtPassword"/>
            <TextBlock Grid.Row="2" Text="Ścieżka firmy:"/>
            <TextBox Grid.Column="1" Grid.Row="2" x:Name="txtDatabasePath"/>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Zaloguj" Margin="5"/>
            <Button Content="Anuluj" Margin="5"/>
        </StackPanel>
    </StackPanel>
</Page>
